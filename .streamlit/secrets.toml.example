# Copy to secrets.toml for local Streamlit, or add in Streamlit Cloud → Settings → Secrets

API_BASE_URL = "http://localhost:8000/api/v1"

# Streamlit Cloud — use your Render backend URL after deploy:
# API_BASE_URL = "https://github-intel-api.onrender.com/api/v1"
